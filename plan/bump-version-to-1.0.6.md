## 任务

将插件版本号从 `1.0.5` 更新到 `1.0.6`。

## 已定位的版本来源

### 1. Maven 主版本号

- 文件：`pom.xml`
- 当前版本：`1.0.5`
- 这是项目的主版本来源

### 2. plugin.yml 版本

- 文件：`src/main/resources/plugin.yml`
- 当前写法：`version: ${project.version}`
- 说明：
  - 不需要手动改成固定值
  - 只要修改 `pom.xml`，构建时会自动注入最终版本

### 3. 构建副本和产物

- 文件：
  - `dependency-reduced-pom.xml`
  - `target/...`
- 这些会在执行 Maven 构建时自动同步

## 本次实现计划

### 1. 更新主版本号

- 修改 `pom.xml` 中的版本为 `1.0.6`

### 2. 保持资源版本注入链路不变

- 不修改 `plugin.yml` 的 `${project.version}` 写法
- 继续让 Maven 负责注入版本

## 预期结果

- 源码版本更新为 `1.0.6`
- 如果之后执行打包，最终 jar 和 `plugin.yml` 也会变为 `1.0.6`

## 待确认

这次我默认只改源码里的版本号，不主动打包。

如果你要我顺手一起重新打包，我确认后会一并执行。
