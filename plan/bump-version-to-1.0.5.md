## 任务

将插件版本号从 `1.0.4` 更新到 `1.0.5`，并整理本轮已完成的更新内容。

## 已定位的版本来源

### 1. Maven 主版本号

- 文件：`pom.xml`
- 当前版本：`1.0.4`
- 这是主要版本来源

### 2. plugin.yml 版本

- 文件：`src/main/resources/plugin.yml`
- 当前写法：`version: ${project.version}`
- 说明：
  - 不需要手动写死版本号
  - 只要更新 `pom.xml`，打包后会自动注入到最终 `plugin.yml`

### 3. 构建产物和缩减 pom

- 文件：
  - `dependency-reduced-pom.xml`
  - `target/...`
- 这些属于构建产物或构建副本
- 如果执行 Maven 打包，会自动同步到 `1.0.5`

## 本次实现计划

### 1. 更新版本号

- 修改 `pom.xml` 中的项目版本为 `1.0.5`

### 2. 构建同步

- 执行 Maven 打包
- 让生成的 jar、`target/classes/plugin.yml`、`dependency-reduced-pom.xml` 同步为 `1.0.5`

### 3. 整理更新说明

- 基于当前这轮已经完成的改动，输出一份简洁的更新内容说明
- 说明范围包括：
  - 历史 GUI 与历史清空
  - 预览页历史入口
  - 历史图标显示
  - 保底 GUI 与 `tiers: {}` 行为修复
  - 交互调整与十连抽
  - PAPI 保底变量
  - 权限/待领取奖励/命令修复等本轮已完成项

## 预期结果

- 项目版本更新为 `1.0.5`
- 新产物文件名变为 `FotiaCrates-1.0.5*.jar`
- 我会同时告诉你这次 `1.0.5` 具体包含了哪些更新

## 待确认

这次我默认会：

- 修改 `pom.xml`
- 执行 Maven 打包
- 用“本轮已完成功能和修复”作为 `1.0.5` 更新说明

确认后开始。
